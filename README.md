# Zenno

Zero-telemetry,  developer toolbox — JSON · Base64 · URL · JWT · regex · diff. SvelteKit + Tailwind, everything runs client-side.

**Live:** <https://nifth.github.io/Zenno/> · Dev: `bun install && bun run dev` · auto-deploys to GitHub Pages on push to `main`.
